# Load graph & app
twitter = loadgraph("$GRPAE_TEST/twitter.yaml")
sssp = loadapp("$GRPAE_TEST/sssp.yaml")

# Input query
sssp.query(4, "sssp_4")

# Unload app & graph
unloadapp(sssp)
unloadgraph(twitter)

# Finally exit
exit
